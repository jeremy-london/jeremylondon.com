#!/usr/bin/env node

console.error(`scripts/expand-blog-posts.mjs has been retired.

It expanded posts from shared sentence banks and category focus templates.
That pattern is forbidden for blog recovery because it creates structurally
similar articles with substituted nouns.

Write posts independently from recovered historical intent instead.`)

process.exit(1)
