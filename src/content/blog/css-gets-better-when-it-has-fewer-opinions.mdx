---
title: "CSS gets better when it has fewer opinions"
excerpt: "Most site styling improves when the design system has fewer special cases and more predictable defaults."
publishDate: "2024-12-04T12:00:00Z"
category: "web-engineering"
draft: false
author: "jeremy-london"
tags: ["css", "design systems", "frontend"]
---

Most site styling improves when the design system has fewer special cases and more predictable defaults. The point is not to make design systems sound bigger than it is. It is to name the decision it changes.

## The Practical Part

Design systems gets interesting when it changes a real workflow. The example I would use is Most site styling improves when the design system has fewer special cases and more predictable defaults, because it gives the reader something to inspect instead of a broad claim to accept.

## The Edge Case

The failure mode for design systems is the page renders but the reader, crawler, or maintainer gets the wrong contract. For design systems, that can look fine from a distance, so the page ships, the demo runs, or the team agrees with itself while the next person still cannot tell what actually happened.

## What I Want To See

The check for design systems is the browser-visible behavior that proves the page still works. For design systems, that is enough to separate a useful note from a long one.

I do not want design systems to sound more certain than the evidence allows. Make the decision visible, then stop.
