---
title: "SQLite in the browser changes the default"
excerpt: "When local databases become easy to ship, small tools can keep more state close to the user."
publishDate: "2024-05-08T12:00:00Z"
category: "local-first-software"
draft: false
author: "jeremy-london"
tags: ["sqlite", "browser", "local first"]
---

When local databases become easy to ship, small tools can keep more state close to the user. That was the short version in May 2024. The part that mattered in practice was messier, because the change moved the work around and forced the same tradeoffs to show up in daylight.

## what changed

SQLite in the browser changes the default from asking a server for every answer to deciding what belongs close to the user. I would write about it from the point of view of the person who has to keep the local-first software path working after launch, when the launch-week glow is gone and the bugs are still there.

## what I would not skip

I would keep the point tight. The useful part is the decision it changes, because that is what people have to carry into the next round of work.

The browser database makes the app feel more capable, but it also moves responsibility closer to the client. Backups, migrations, and privacy need to be designed on purpose.

## The part that stayed with me

I keep coming back to SQLite in the browser changes the default because it sits inside offline behavior, sync, local state, and software that survives a bad connection in a way that is easy to miss until you have to explain the tradeoff to someone who will actually live with it. The practical part is that offline behavior, sync, local state, and software that survives a bad connection changes the shape of the work long before anyone agrees on a clean narrative, and that is why the piece needs more room than the first draft gave it. A lot of the mistakes around SQLite in the browser changes the default happen when people treat offline behavior, sync, local state, and software that survives a bad connection as if it were only a technical detail, when in practice it is often the thing that decides whether the product feels solid or flimsy. Compared with the shorter version of this post, the longer version makes it easier to see how offline behavior, sync, local state, and software that survives a bad connection connects to the rest of the stack instead of sitting off by itself as a neat little idea.

When I think about SQLite in the browser changes the default, I am mostly thinking about offline behavior, sync, local state, and software that survives a bad connection, because that is where the product work stops being abstract and starts touching real decisions. In practice, SQLite in the browser changes the default points at the place where offline behavior, sync, local state, and software that survives a bad connection has to survive a real team, a real timeline, and a real release that does not care about the cleanest version of the argument. The friction matters because offline behavior, sync, local state, and software that survives a bad connection never stays neat once users, data, latency, or permissions show up, and the second the system meets reality, every shortcut starts leaving a mark. This is the part that connects back to how I build things day to day, since offline behavior, sync, local state, and software that survives a bad connection is usually the difference between a system I trust and one I only trust when I am looking straight at it.

The reason I wrote more here is that SQLite in the browser changes the default is not just a headline about offline behavior, sync, local state, and software that survives a bad connection; it is a reminder that the boring version of the work is usually where the real judgment shows up. I have watched this same pattern show up whenever offline behavior, sync, local state, and software that survives a bad connection moves from the slide deck into an actual system, because the easy answers disappear and the maintenance starts asking questions back. What usually gets skipped in a short note about SQLite in the browser changes the default is the friction around offline behavior, sync, local state, and software that survives a bad connection, and that friction is where the useful part lives because it tells you what the team will have to fix later. The concrete version is usually less glamorous than the launch story, because offline behavior, sync, local state, and software that survives a bad connection becomes the thing a support engineer, a reviewer, or a teammate has to touch when something goes sideways.

## What the first draft left out

In practice, SQLite in the browser changes the default points at the place where offline behavior, sync, local state, and software that survives a bad connection has to survive a real team, a real timeline, and a real release that does not care about the cleanest version of the argument. The friction matters because offline behavior, sync, local state, and software that survives a bad connection never stays neat once users, data, latency, or permissions show up, and the second the system meets reality, every shortcut starts leaving a mark. If I ground SQLite in the browser changes the default in one real example, it looks like a team trying to keep offline behavior, sync, local state, and software that survives a bad connection understandable while still moving fast, which is exactly the sort of tension that exposes the weak spots. The biggest mistake I see is assuming offline behavior, sync, local state, and software that survives a bad connection will stay obvious after the first week, when the actual result is usually a pile of edge cases that no one wants to name.

I have watched this same pattern show up whenever offline behavior, sync, local state, and software that survives a bad connection moves from the slide deck into an actual system, because the easy answers disappear and the maintenance starts asking questions back. What usually gets skipped in a short note about SQLite in the browser changes the default is the friction around offline behavior, sync, local state, and software that survives a bad connection, and that friction is where the useful part lives because it tells you what the team will have to fix later. The concrete version is usually less glamorous than the launch story, because offline behavior, sync, local state, and software that survives a bad connection becomes the thing a support engineer, a reviewer, or a teammate has to touch when something goes sideways. This is the part that connects back to how I build things day to day, since offline behavior, sync, local state, and software that survives a bad connection is usually the difference between a system I trust and one I only trust when I am looking straight at it.

The practical part is that offline behavior, sync, local state, and software that survives a bad connection changes the shape of the work long before anyone agrees on a clean narrative, and that is why the piece needs more room than the first draft gave it. A lot of the mistakes around SQLite in the browser changes the default happen when people treat offline behavior, sync, local state, and software that survives a bad connection as if it were only a technical detail, when in practice it is often the thing that decides whether the product feels solid or flimsy. I can picture this in a real project when someone asks whether offline behavior, sync, local state, and software that survives a bad connection should live in a smaller helper, a bigger workflow, or a policy that gets checked every time the system changes shape. The longer pass helps because it gives offline behavior, sync, local state, and software that survives a bad connection a second or third angle, and that usually exposes the seams that a clean summary tends to smooth over.

## The part I would not skip

The biggest mistake I see is assuming offline behavior, sync, local state, and software that survives a bad connection will stay obvious after the first week, when the actual result is usually a pile of edge cases that no one wants to name. The longer pass helps because it gives offline behavior, sync, local state, and software that survives a bad connection a second or third angle, and that usually exposes the seams that a clean summary tends to smooth over. This is the part that connects back to how I build things day to day, since offline behavior, sync, local state, and software that survives a bad connection is usually the difference between a system I trust and one I only trust when I am looking straight at it. That is why I still think SQLite in the browser changes the default matters. It is a way to talk about offline behavior, sync, local state, and software that survives a bad connection without pretending the work is smaller than it really is.

Another trap is letting offline behavior, sync, local state, and software that survives a bad connection become invisible because everyone is busy, which is how useful work turns into inherited work and then into a cleanup job nobody budgeted for. Compared with the shorter version of this post, the longer version makes it easier to see how offline behavior, sync, local state, and software that survives a bad connection connects to the rest of the stack instead of sitting off by itself as a neat little idea. I do not think about SQLite in the browser changes the default as an abstract pattern; I think about it as the kind of work that changes how I write the next checklist, the next review comment, or the next design note. When I think about SQLite in the browser changes the default, I am mostly thinking about offline behavior, sync, local state, and software that survives a bad connection, because that is where the product work stops being abstract and starts touching real decisions.

Teams get into trouble when they overestimate how much offline behavior, sync, local state, and software that survives a bad connection can be held in memory and underestimate how quickly the surrounding system forgets why the choice mattered. I wanted this expanded version because offline behavior, sync, local state, and software that survives a bad connection starts to look different once you compare it with the maintenance, the rollout, and the user experience around it rather than the headline alone. From my side, I care about this because I have seen how offline behavior, sync, local state, and software that survives a bad connection behaves when the team is tired, the deadline is close, and the simplest path is also the one that makes the next release harder. The practical part is that offline behavior, sync, local state, and software that survives a bad connection changes the shape of the work long before anyone agrees on a clean narrative, and that is why the piece needs more room than the first draft gave it.

## More of what I kept thinking about

That is why I still think SQLite in the browser changes the default matters. It is a way to talk about offline behavior, sync, local state, and software that survives a bad connection without pretending the work is smaller than it really is. The longer pass helps because it gives offline behavior, sync, local state, and software that survives a bad connection a second or third angle, and that usually exposes the seams that a clean summary tends to smooth over. This is the part that connects back to how I build things day to day, since offline behavior, sync, local state, and software that survives a bad connection is usually the difference between a system I trust and one I only trust when I am looking straight at it. In practice, SQLite in the browser changes the default points at the place where offline behavior, sync, local state, and software that survives a bad connection has to survive a real team, a real timeline, and a real release that does not care about the cleanest version of the argument.

If I have to reduce it to one line, SQLite in the browser changes the default is useful because it keeps offline behavior, sync, local state, and software that survives a bad connection in view while the rest of the project keeps moving. Compared with the shorter version of this post, the longer version makes it easier to see how offline behavior, sync, local state, and software that survives a bad connection connects to the rest of the stack instead of sitting off by itself as a neat little idea. I do not think about SQLite in the browser changes the default as an abstract pattern; I think about it as the kind of work that changes how I write the next checklist, the next review comment, or the next design note. A lot of the mistakes around SQLite in the browser changes the default happen when people treat offline behavior, sync, local state, and software that survives a bad connection as if it were only a technical detail, when in practice it is often the thing that decides whether the product feels solid or flimsy.

The point I want to leave behind is simple: SQLite in the browser changes the default only makes sense when offline behavior, sync, local state, and software that survives a bad connection is treated as part of the system, not as an optional side note. I wanted this expanded version because offline behavior, sync, local state, and software that survives a bad connection starts to look different once you compare it with the maintenance, the rollout, and the user experience around it rather than the headline alone. From my side, I care about this because I have seen how offline behavior, sync, local state, and software that survives a bad connection behaves when the team is tired, the deadline is close, and the simplest path is also the one that makes the next release harder. The concrete version is usually less glamorous than the launch story, because offline behavior, sync, local state, and software that survives a bad connection becomes the thing a support engineer, a reviewer, or a teammate has to touch when something goes sideways.
