---
title: "SQLite in the browser changes the default"
excerpt: "When local databases become easy to ship, small tools can keep more state close to the user."
publishDate: "2024-05-08T12:00:00Z"
category: "local-first-software"
draft: false
author: "jeremy-london"
tags: ["sqlite", "browser", "local first"]
---

When local databases become easy to ship, small tools can keep more state close to the user. I would keep the argument close to the workflow instead of treating SQLite like a slogan.

## Why It Matters

For SQLite, the important part is When local databases become easy to ship, small tools can keep more state close to the user. That gives the idea a place to stand. Without that concrete surface, the post turns into category talk: offline behavior, sync, local state, and private work.

## The Risk

The bad version of SQLite is familiar: the app works online but loses the user once the network or device state changes. For SQLite, sometimes that means a decision with no trail, a boundary with no owner, or an example that does not prove the point.

## My Bar

For SQLite, I would ask for the rule for what happens before the server is reachable. The SQLite post can be brief and still be useful if that evidence is present.

The smaller version is stronger. It gives the reader the SQLite claim, the failure mode, and the check without pretending there is a larger theory hiding underneath.
