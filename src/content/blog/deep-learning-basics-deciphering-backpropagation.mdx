---
title: "Backpropagation is bookkeeping with consequences"
excerpt: "A slower explanation of gradients, loss, and weight updates for people who want the mechanism without the fog machine."
publishDate: "2024-04-12T00:00:00Z"
category: "deep-learning-basics"
draft: false
author: "jeremy-london"
tags: ["backpropagation", "gradient descent", "training"]
load_pyodide: true
load_mathjax: true
---

import PythonModule from "@components/blog/shared/PythonModule.jsx";
import BackPropagation from "@components/blog/BackPropagation.jsx";

A slower explanation of gradients, loss, and weight updates for people who want the mechanism without the fog machine. For backpropagation, the useful move is to slow down and keep the numbers visible. A neural network becomes less mysterious when each multiplication, bias, and activation has somewhere to live on the page.

## Start with the shape

I would start with a slower explanation of gradients, loss, and weight updates for people who want the mechanism without the fog machine. Name the inputs. Name the weights. Write the bias down instead of waving at it. The point is not to make the math look impressive; it is to make the next step hard to hide.

Backpropagation works better as a learning object when the reader can change one value and see what moves. If every value changes at once, the lesson turns into fog. If one value changes and the output follows, the model starts to feel inspectable.

## Run the example

<BackPropagation client:only="react" />
<PythonModule
  client:only="react"
  filePath="examples/basics/backpropagation.py"
  outputRows={32}
/>

## What to watch

The mistake to watch for in backpropagation is treating the output as magic because the notation got dense. For these small examples, the check is plain: compute the dot product, add the bias, apply the activation, and compare the result with the rendered demo.

That is also why the runnable version matters here. A static explanation can be correct and still leave the reader with no grip. A small interactive example gives them a place to poke the system and find the edge.

## Where I land

I do not need backpropagation to feel profound. I need it to be traceable. If the reader can explain which input mattered, which weight amplified it, and where the activation changed the result, the post has done its job.
