---
title: "Tailwind 4 made CSS configuration feel like CSS"
excerpt: "Tailwind CSS v4 moved more of the system into CSS itself, which made configuration feel less separate from styling."
publishDate: "2025-01-29T12:00:00Z"
category: "web-engineering"
draft: false
author: "jeremy-london"
tags: ["tailwind", "css", "frontend"]
---

Tailwind CSS v4 moved more of the system into CSS itself, which made configuration feel less separate from styling. The useful version is practical: Tailwind CSS v4 moved more of the system into CSS itself, which made configuration feel less separate from styling.

## What Changes

For tailwind, I would start from the constraint, not the announcement. The constraint is Tailwind CSS v4 moved more of the system into CSS itself, which made configuration feel less separate from styling, and the surrounding question is how content, forms, feeds, frontend behavior, and small-site maintenance changes in practice.

## Where It Fails

The thing I would watch for with tailwind is quieter than a crash: the page renders but the reader, crawler, or maintainer gets the wrong contract. That is the kind of break that makes a tailwind post feel padded even when the topic is legitimate.

## The Check

My bar for tailwind is the browser-visible behavior that proves the page still works. If the tailwind post cannot show that, it should stay short and honest.

That is where I would leave it. Keep the tailwind claim narrow, make the tradeoff visible, and let the example do the work.
