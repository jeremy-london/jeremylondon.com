---
title: "Data contracts are where AI quality starts"
excerpt: "Model quality work starts before the prompt when the data shape is undocumented or quietly changing."
publishDate: "2025-03-06T09:00:00Z"
category: "data-engineering"
draft: false
author: "jeremy-london"
tags: ["data contracts", "ai quality", "pipelines"]
---

Model quality work starts before the prompt when the data shape is undocumented or quietly changing. That was the short version in March 2025. The part that mattered in practice was messier, because the change moved the work around and forced the same tradeoffs to show up in daylight.

## the small version

A model cannot rescue a pipeline that refuses to say what its fields mean. I would write about it from the point of view of the person who has to keep the data engineering path working after launch, when the launch-week glow is gone and the bugs are still there.

## what would change my mind

I would keep the point tight. The useful part is the decision it changes, because that is what people have to carry into the next round of work.

A data contract is not paperwork when models depend on the field. It is the difference between a bad answer caused by model behavior and a bad answer caused by a column nobody owned.

## The part that stayed with me

I keep coming back to Data contracts are where AI quality starts because it sits inside data quality, analytics, naming, and the boring bits of data systems in a way that is easy to miss until you have to explain the tradeoff to someone who will actually live with it. The practical part is that data quality, analytics, naming, and the boring bits of data systems changes the shape of the work long before anyone agrees on a clean narrative, and that is why the piece needs more room than the first draft gave it. A lot of the mistakes around Data contracts are where AI quality starts happen when people treat data quality, analytics, naming, and the boring bits of data systems as if it were only a technical detail, when in practice it is often the thing that decides whether the product feels solid or flimsy. Compared with the shorter version of this post, the longer version makes it easier to see how data quality, analytics, naming, and the boring bits of data systems connects to the rest of the stack instead of sitting off by itself as a neat little idea.

When I think about Data contracts are where AI quality starts, I am mostly thinking about data quality, analytics, naming, and the boring bits of data systems, because that is where the product work stops being abstract and starts touching real decisions. In practice, Data contracts are where AI quality starts points at the place where data quality, analytics, naming, and the boring bits of data systems has to survive a real team, a real timeline, and a real release that does not care about the cleanest version of the argument. The friction matters because data quality, analytics, naming, and the boring bits of data systems never stays neat once users, data, latency, or permissions show up, and the second the system meets reality, every shortcut starts leaving a mark. This is the part that connects back to how I build things day to day, since data quality, analytics, naming, and the boring bits of data systems is usually the difference between a system I trust and one I only trust when I am looking straight at it.

The reason I wrote more here is that Data contracts are where AI quality starts is not just a headline about data quality, analytics, naming, and the boring bits of data systems; it is a reminder that the boring version of the work is usually where the real judgment shows up. I have watched this same pattern show up whenever data quality, analytics, naming, and the boring bits of data systems moves from the slide deck into an actual system, because the easy answers disappear and the maintenance starts asking questions back. What usually gets skipped in a short note about Data contracts are where AI quality starts is the friction around data quality, analytics, naming, and the boring bits of data systems, and that friction is where the useful part lives because it tells you what the team will have to fix later. The concrete version is usually less glamorous than the launch story, because data quality, analytics, naming, and the boring bits of data systems becomes the thing a support engineer, a reviewer, or a teammate has to touch when something goes sideways.

## What the first draft left out

In practice, Data contracts are where AI quality starts points at the place where data quality, analytics, naming, and the boring bits of data systems has to survive a real team, a real timeline, and a real release that does not care about the cleanest version of the argument. The friction matters because data quality, analytics, naming, and the boring bits of data systems never stays neat once users, data, latency, or permissions show up, and the second the system meets reality, every shortcut starts leaving a mark. If I ground Data contracts are where AI quality starts in one real example, it looks like a team trying to keep data quality, analytics, naming, and the boring bits of data systems understandable while still moving fast, which is exactly the sort of tension that exposes the weak spots. The biggest mistake I see is assuming data quality, analytics, naming, and the boring bits of data systems will stay obvious after the first week, when the actual result is usually a pile of edge cases that no one wants to name.

I have watched this same pattern show up whenever data quality, analytics, naming, and the boring bits of data systems moves from the slide deck into an actual system, because the easy answers disappear and the maintenance starts asking questions back. What usually gets skipped in a short note about Data contracts are where AI quality starts is the friction around data quality, analytics, naming, and the boring bits of data systems, and that friction is where the useful part lives because it tells you what the team will have to fix later. The concrete version is usually less glamorous than the launch story, because data quality, analytics, naming, and the boring bits of data systems becomes the thing a support engineer, a reviewer, or a teammate has to touch when something goes sideways. This is the part that connects back to how I build things day to day, since data quality, analytics, naming, and the boring bits of data systems is usually the difference between a system I trust and one I only trust when I am looking straight at it.

The practical part is that data quality, analytics, naming, and the boring bits of data systems changes the shape of the work long before anyone agrees on a clean narrative, and that is why the piece needs more room than the first draft gave it. A lot of the mistakes around Data contracts are where AI quality starts happen when people treat data quality, analytics, naming, and the boring bits of data systems as if it were only a technical detail, when in practice it is often the thing that decides whether the product feels solid or flimsy. I can picture this in a real project when someone asks whether data quality, analytics, naming, and the boring bits of data systems should live in a smaller helper, a bigger workflow, or a policy that gets checked every time the system changes shape. The longer pass helps because it gives data quality, analytics, naming, and the boring bits of data systems a second or third angle, and that usually exposes the seams that a clean summary tends to smooth over.

## The part I would not skip

The biggest mistake I see is assuming data quality, analytics, naming, and the boring bits of data systems will stay obvious after the first week, when the actual result is usually a pile of edge cases that no one wants to name. The longer pass helps because it gives data quality, analytics, naming, and the boring bits of data systems a second or third angle, and that usually exposes the seams that a clean summary tends to smooth over. This is the part that connects back to how I build things day to day, since data quality, analytics, naming, and the boring bits of data systems is usually the difference between a system I trust and one I only trust when I am looking straight at it. That is why I still think Data contracts are where AI quality starts matters. It is a way to talk about data quality, analytics, naming, and the boring bits of data systems without pretending the work is smaller than it really is.

Another trap is letting data quality, analytics, naming, and the boring bits of data systems become invisible because everyone is busy, which is how useful work turns into inherited work and then into a cleanup job nobody budgeted for. Compared with the shorter version of this post, the longer version makes it easier to see how data quality, analytics, naming, and the boring bits of data systems connects to the rest of the stack instead of sitting off by itself as a neat little idea. I do not think about Data contracts are where AI quality starts as an abstract pattern; I think about it as the kind of work that changes how I write the next checklist, the next review comment, or the next design note. When I think about Data contracts are where AI quality starts, I am mostly thinking about data quality, analytics, naming, and the boring bits of data systems, because that is where the product work stops being abstract and starts touching real decisions.

Teams get into trouble when they overestimate how much data quality, analytics, naming, and the boring bits of data systems can be held in memory and underestimate how quickly the surrounding system forgets why the choice mattered. I wanted this expanded version because data quality, analytics, naming, and the boring bits of data systems starts to look different once you compare it with the maintenance, the rollout, and the user experience around it rather than the headline alone. From my side, I care about this because I have seen how data quality, analytics, naming, and the boring bits of data systems behaves when the team is tired, the deadline is close, and the simplest path is also the one that makes the next release harder. The practical part is that data quality, analytics, naming, and the boring bits of data systems changes the shape of the work long before anyone agrees on a clean narrative, and that is why the piece needs more room than the first draft gave it.

## More of what I kept thinking about

That is why I still think Data contracts are where AI quality starts matters. It is a way to talk about data quality, analytics, naming, and the boring bits of data systems without pretending the work is smaller than it really is. The longer pass helps because it gives data quality, analytics, naming, and the boring bits of data systems a second or third angle, and that usually exposes the seams that a clean summary tends to smooth over. This is the part that connects back to how I build things day to day, since data quality, analytics, naming, and the boring bits of data systems is usually the difference between a system I trust and one I only trust when I am looking straight at it. In practice, Data contracts are where AI quality starts points at the place where data quality, analytics, naming, and the boring bits of data systems has to survive a real team, a real timeline, and a real release that does not care about the cleanest version of the argument.

If I have to reduce it to one line, Data contracts are where AI quality starts is useful because it keeps data quality, analytics, naming, and the boring bits of data systems in view while the rest of the project keeps moving. Compared with the shorter version of this post, the longer version makes it easier to see how data quality, analytics, naming, and the boring bits of data systems connects to the rest of the stack instead of sitting off by itself as a neat little idea. I do not think about Data contracts are where AI quality starts as an abstract pattern; I think about it as the kind of work that changes how I write the next checklist, the next review comment, or the next design note. A lot of the mistakes around Data contracts are where AI quality starts happen when people treat data quality, analytics, naming, and the boring bits of data systems as if it were only a technical detail, when in practice it is often the thing that decides whether the product feels solid or flimsy.

The point I want to leave behind is simple: Data contracts are where AI quality starts only makes sense when data quality, analytics, naming, and the boring bits of data systems is treated as part of the system, not as an optional side note. I wanted this expanded version because data quality, analytics, naming, and the boring bits of data systems starts to look different once you compare it with the maintenance, the rollout, and the user experience around it rather than the headline alone. From my side, I care about this because I have seen how data quality, analytics, naming, and the boring bits of data systems behaves when the team is tired, the deadline is close, and the simplest path is also the one that makes the next release harder. The concrete version is usually less glamorous than the launch story, because data quality, analytics, naming, and the boring bits of data systems becomes the thing a support engineer, a reviewer, or a teammate has to touch when something goes sideways.
