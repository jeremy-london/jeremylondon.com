---
title: "AI incident reviews need model context"
excerpt: "An AI incident review has to include prompt, model, retrieved context, tool calls, and approval state."
publishDate: "2026-04-22T14:00:00Z"
category: "ai-operations"
draft: false
author: "jeremy-london"
tags: ["incident review", "ai ops", "models"]
---

{/*
Depth: quick-note
Information map:
- the concrete incident review problem
- the example surface: a user deciding whether an agent should be allowed to continue
- the failure mode: a model path changes and the team notices only after user behavior drifts
- the small decision I would keep
- what would make the note wrong
*/}

An AI incident review has to include prompt, model, retrieved context, tool calls, and approval state.

I would keep incident review small here. The useful part is a user deciding whether an agent should be allowed to continue. That is enough material for a incident review note, not a manifesto.

The incident review mistake is treating the observation as if it needs a complete theory around it. In practice, AI operations work usually gets better when the first version names the boundary and then stops. For incident review, that boundary is what the model can change without human review.

The incident review check I would use is simple: can the next person see what changed and why it matters? If the answer is yes, the incident review post has done its job. If the answer is no, adding three more incident review paragraphs will not fix it.

The thing I would not add to incident review is a fake architecture section. A short incident review note can still be technical if it names the pressure clearly: who is acting, what state is being touched, and what evidence would make the claim falsifiable. That is enough for incident review. The rest of incident review can wait until there is a real example to inspect.

I am leaving incident review as a short note because the idea is useful at this size. A longer incident review version would need a real system diagram, a failure example, and evidence. Without those, more length would just be decoration.
