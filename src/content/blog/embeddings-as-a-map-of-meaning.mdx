---
title: "Embeddings are product plumbing"
excerpt: "Embeddings are useful because they make search, memory, deduping, and retrieval feel less brittle."
publishDate: "2024-06-09T16:00:00Z"
category: "deep-learning-principals"
draft: false
author: "jeremy-london"
tags: ["embeddings", "semantic search", "vectors"]
---

Embeddings are useful because they make search, memory, deduping, and retrieval feel less brittle. The point is not to make embeddings sound bigger than it is. It is to name the decision it changes.

## The Practical Part

Embeddings gets interesting when it changes a real workflow. The example I would use is Embeddings are useful because they make search, memory, deduping, and retrieval feel less brittle, because it gives the reader something to inspect instead of a broad claim to accept.

## The Edge Case

The failure mode for embeddings is the explanation sounds plausible while the actual decision stays vague. For embeddings, that can look fine from a distance, so the page ships, the demo runs, or the team agrees with itself while the next person still cannot tell what actually happened.

## What I Want To See

The check for embeddings is the example or constraint that makes the point inspectable. For embeddings, that is enough to separate a useful note from a long one.

I do not want embeddings to sound more certain than the evidence allows. Make the decision visible, then stop.
