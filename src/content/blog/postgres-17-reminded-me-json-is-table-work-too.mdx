---
title: "Postgres 17 reminded me JSON is table work too"
excerpt: "Postgres 17 put more SQL/JSON work into the database, which made messy application data feel less separate from relational design."
publishDate: "2024-10-02T11:00:00Z"
category: "data-engineering"
draft: false
author: "jeremy-london"
tags: ["postgres", "json", "data"]
---

Postgres 17 put more SQL/JSON work into the database, which made messy application data feel less separate from relational design. I care about postgres only when it changes the work someone has to do.

## The Useful Version

The move that matters for postgres is small: connect the idea to Postgres 17 put more SQL/JSON work into the database, which made messy application data feel less separate from relational design. Once postgres is visible, the reader can tell whether this claim is operational or just commentary.

## The Failure Mode

Postgres breaks when the pipeline produces output after the meaning of a field has moved. Once postgres reaches that point, the language may still sound confident, but the system has stopped giving the reader evidence.

## The Test

The version I would publish needs the contract that explains what changed. Without that evidence for postgres, I would rather cut the paragraph than decorate the uncertainty.

This works best as a concise note. The postgres claim does not need more volume; it needs one clear boundary and one concrete reason to care.
