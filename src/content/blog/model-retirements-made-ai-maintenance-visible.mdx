---
title: "Model retirements made AI maintenance visible"
excerpt: "By early 2026, model menus and deprecation dates made it obvious that AI applications need maintenance plans."
publishDate: "2026-02-16T12:00:00Z"
category: "deep-learning-principals"
draft: false
author: "jeremy-london"
tags: ["models", "maintenance", "migration"]
---

{/*
Depth: quick-note
Information map:
- the concrete models problem
- the example surface: By early 2026, model menus and deprecation dates made it obvious that AI applications need maintenance plans
- the failure mode: the system succeeds once but gives the next maintainer no way to explain it
- the small decision I would keep
- what would make the note wrong
*/}

By early 2026, model menus and deprecation dates made it obvious that AI applications need maintenance plans.

I would keep models small here. The useful part is By early 2026, model menus and deprecation dates made it obvious that AI applications need maintenance plans. That is enough material for a models note, not a manifesto.

The models mistake is treating the observation as if it needs a complete theory around it. In practice, engineering quality work usually gets better when the first version names the boundary and then stops. For models, that boundary is what must be proven before the change is trusted.

The models check I would use is simple: can the next person see what changed and why it matters? If the answer is yes, the models post has done its job. If the answer is no, adding three more models paragraphs will not fix it.

The thing I would not add to models is a fake architecture section. A short models note can still be technical if it names the pressure clearly: who is acting, what state is being touched, and what evidence would make the claim falsifiable. That is enough for models. The rest of models can wait until there is a real example to inspect.

I am leaving models as a short note because the idea is useful at this size. A longer models version would need a real system diagram, a failure example, and evidence. Without those, more length would just be decoration.
