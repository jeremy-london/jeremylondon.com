---
title: "Structured Outputs made prompts less magical"
excerpt: "OpenAI Structured Outputs moved a common integration problem from prompt superstition into schema work."
publishDate: "2024-08-12T11:00:00Z"
category: "deep-learning-principals"
draft: false
author: "jeremy-london"
tags: ["structured outputs", "schemas", "openai"]
---

OpenAI Structured Outputs moved a common integration problem from prompt superstition into schema work. I would keep the argument close to the workflow instead of treating structured outputs like a slogan.

## Why It Matters

For structured outputs, the important part is OpenAI Structured Outputs moved a common integration problem from prompt superstition into schema work. That gives the idea a place to stand. Without that concrete surface, the post turns into category talk: constraints, examples, and maintenance.

## The Risk

The bad version of structured outputs is familiar: the explanation sounds plausible while the actual decision stays vague. For structured outputs, sometimes that means a decision with no trail, a boundary with no owner, or an example that does not prove the point.

## My Bar

For structured outputs, I would ask for the example or constraint that makes the point inspectable. The structured outputs post can be brief and still be useful if that evidence is present.

The smaller version is stronger. It gives the reader the structured outputs claim, the failure mode, and the check without pretending there is a larger theory hiding underneath.
