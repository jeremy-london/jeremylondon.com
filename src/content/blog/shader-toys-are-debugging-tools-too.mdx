---
title: "Shader toys are debugging tools too"
excerpt: "Small visual programs are not only toys. They are a way to make math and state visible."
publishDate: "2025-04-09T11:00:00Z"
category: "creative-coding"
draft: false
author: "jeremy-london"
tags: ["shaders", "webgpu", "visualization"]
---

Small visual programs are not only toys. They are a way to make math and state visible. The point is not to make shaders sound bigger than it is. It is to name the decision it changes.

## The Practical Part

Shaders gets interesting when it changes a real workflow. The example I would use is Small visual programs are not only toys. They are a way to make math and state visible, because it gives the reader something to inspect instead of a broad claim to accept.

## The Edge Case

The failure mode for shaders is the visual is interesting but does not teach the system underneath. For shaders, that can look fine from a distance, so the page ships, the demo runs, or the team agrees with itself while the next person still cannot tell what actually happened.

## What I Want To See

The check for shaders is the control that lets the reader change the result. For shaders, that is enough to separate a useful note from a long one.

I do not want shaders to sound more certain than the evidence allows. Make the decision visible, then stop.
